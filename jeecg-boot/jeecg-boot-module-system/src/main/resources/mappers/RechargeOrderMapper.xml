<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.game.mapper.RechargeOrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.jeecg.modules.game.entity.RechargeOrder">
        <id column="id" property="id"/>
        <result column="goods_id" property="goodsId"/>
        <result column="goods_type" property="goodsType"/>
        <result column="dau" property="dau"/>
    </resultMap>


    <select id="queryGiftList" resultMap="BaseResultMap">
        SELECT
        DISTINCT(goods_id),
        goods_type
        FROM recharge_order
        WHERE
        create_date >= DATE (#{rangeDateBeginTime}) AND create_date &lt;= DATE (#{rangeDateEndTime})
        AND goods_type= #{goodsType}
    </select>

    <select id="queryDAU" resultType="Long">
        SELECT
        COUNT(DISTINCT(player_id)) dau
        FROM ${logTable}
        WHERE
        create_date >= DATE (#{rangeDateBeginTime}) AND create_date &lt;= DATE (#{rangeDateEndTime})
        AND server_id = #{serverId}
        AND channel = #{channel}

    </select>
</mapper>