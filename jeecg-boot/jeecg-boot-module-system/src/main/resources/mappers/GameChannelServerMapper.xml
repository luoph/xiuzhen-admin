<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.game.mapper.GameChannelServerMapper">

    <select id="gameServerByChannelId" resultType="org.jeecg.modules.game.entity.GameServer">
        SELECT a.* FROM `game_server` a
        INNER JOIN `game_channel_server` b
        ON a.`id` = b.`server_id`
        WHERE b.`channel_id` =  #{channelId} AND b.`del_flag` = 0 AND b.`no_need_count` = 0;
    </select>
</mapper>